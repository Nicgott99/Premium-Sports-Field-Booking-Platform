export class MetricsCollector { constructor() { this.metrics = {}; } record(name, value) { if (!this.metrics[name]) this.metrics[name] = []; this.metrics[name].push(value); } getAverage(name) { if (!this.metrics[name]) return 0; const sum = this.metrics[name].reduce((a, b) => a + b, 0); return sum / this.metrics[name].length; } getMax(name) { return Math.max(...(this.metrics[name] || [0])); } getMin(name) { return Math.min(...(this.metrics[name] || [0])); } clear() { this.metrics = {}; } }
